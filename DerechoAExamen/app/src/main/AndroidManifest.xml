<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.duke.derechoaexamen">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity_lista">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".QR" />
        <activity android:name=".Intents" />
        <activity android:name=".lectura" />
        <activity android:name=".Dialogs" />
        <activity android:name=".ActividadPrincipal" />
        <activity android:name=".Cerrar" />
        <activity android:name=".datosconcatenados"></activity>
    </application>

</manifest>